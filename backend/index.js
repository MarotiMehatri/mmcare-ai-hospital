import app from "./app.js";

/**
 * Vercel Express Entry Point
 *
 * Vercel imports this Express application
 * and handles the HTTP server automatically.
 *
 * Do NOT use app.listen() here.
 */

export default app;